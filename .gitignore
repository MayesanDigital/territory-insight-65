# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Datos fuente descargados del INE/INEGI. Son públicos pero pesados y
# reproducibles con scripts/descargar-ine.ps1; no tienen por qué versionarse.
data/

# Estado local de la CLI de Supabase (ref enlazada, versiones detectadas).
supabase/.temp/

# Respaldos de la base. Contienen datos personales de contactos y perfiles, así
# que no pueden versionarse ni acabar en un repositorio, público o privado.
respaldos/

# Environment — nunca versionar secretos. Usa .env.example como plantilla.
.env
.env.*
!.env.example
.vercel

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
